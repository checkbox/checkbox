#!/bin/bash

export XDG_CACHE_HOME=${XDG_CACHE_HOME:-$HOME/.cache}
export CHECKBOX_DATA=${CHECKBOX_DATA:-.}
export CHECKBOX_SHARE=${CHECKBOX_SHARE:-.}
export CHECKBOX_OPTIONS="--log-level=debug --log=/var/log/checkbox.log"

python $CHECKBOX_SHARE/run "$@" $CHECKBOX_SHARE/configs/$(basename $0).ini
